@namespace Jumpeno.Client

@inject CookieStorage CookieStorage

@code {
    // Views ------------------------------------------------------------------------------------------------------------------------------
    private ThemeProvider ThemeProviderRef = null!;
    private CookieModal CookieModalRef = null!;
    public void Notify() => StateHasChanged();

    // Lifecycle --------------------------------------------------------------------------------------------------------------------------
    protected override void OnInitialized() => AuthPage.Init(this);

    protected override async Task OnAfterRenderAsync(bool firstRender) {
        try {
            if (!firstRender) { AuthPage.FinishStage(); return; }
            // [Before layout rendered] ---------------------------------------------------------------------------------------------------
            // Import:
            await ClientImport.Await();
            // Authorization:
            await HTTP.Sync(async () => {
                if (!await Auth.TryLogInAdmin()) await Auth.TryLogInToken();
            });
            // Culture:
            await SelectCulture.Init();
            // Init actions:
            await Init.RunBeforeLayout();
            // Authorization [layout render]:
            await AuthPage.NextStage();
            await AuthPage.ChangePath();
            await AuthPage.AddRedirect();
            // Cookie modal:
            // Only show cookie modal if no cookies have been accepted.
            // User needs to accept at least mandatory cookies to use the app.
            bool shouldCookiesModalShow = await HTTP.Sync(() => CookieStorage.GetAcceptedCookies().Count) <= 0;
            if (shouldCookiesModalShow) await CookieModal.Open(true);
            // [After layout rendered] ----------------------------------------------------------------------------------------------------
            // Theme:
            await ThemeProviderRef.Initialization;
            // Init actions:
            await Init.RunAfterLayout();
            // Server loader:
            ServerPageLoader.Hide();
            // Init actions:
            await Init.RunAfterLoader();
            await PageLoader.Hide(PageLoaderTask.INITIAL, false);
            // PageLoader:
            await PageLoader.WithActiveLoader(() => ActionHandler.SetFocus(PageLoader.ID));
            await PageLoader.AwaitAllLoaders();
            // Focus:
            if (shouldCookiesModalShow)
                ActionHandler.SetFocus(CookieModalRef.GetDialogID());
            else
                ActionHandler.SetFocus(WebDocument.ID);
        } catch (AppException e) {
            // App error:
            ServerPageLoader.Stop();
            Notification.Error(e.Info.T);
        } catch {
            // Initialization error:
            ServerPageLoader.Stop();
            Notification.Error(Messages.DEFAULT.T);
        }
    }
}

<SSRStorage />
<AnimationHandler />
<UI />
<ThemeProvider @ref="ThemeProviderRef">
    <ImagePreloader />
    <CanvasPreRenderer />
    <AriaPageAlert />
    <PageLoader>
        <ModalProvider>
            <WebDocument>
                <DocTitle Value="@AppSettings.Name" />
                <AuthPage>
                    <AppLayout>
                        <Router AppAssembly="@typeof(App).Assembly">
                            <Found Context="routeData">
                                <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
                            </Found>
                            <NotFound>
                                <LayoutView Layout="@typeof(MainLayout)">
                                    <Error404Page />
                                </LayoutView>
                            </NotFound>
                        </Router>
                    </AppLayout>
                </AuthPage>
            </WebDocument>
        </ModalProvider>
        <AntDesign.AntContainer />
        <CookieModal @ref="CookieModalRef" />
    </PageLoader>
    <Notification />
</ThemeProvider>
