@namespace Jumpeno.Client.Components

@inherits AuthComponent

@code {
    [Parameter]
    public Role[] Roles { get; set; } = [Role.USER, Role.ADMIN];
    [Parameter]
    public required RenderFragment ChildContent { get; set; }
}

@if (Auth.IsRole(Roles)) {
    @ChildContent
}
