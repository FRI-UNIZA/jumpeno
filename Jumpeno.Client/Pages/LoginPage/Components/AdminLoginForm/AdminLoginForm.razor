@namespace Jumpeno.Client.Components

@inherits BasicComponent

<DocTitle Value="@I18N.T("Admin")" />

@if (!Verified) {
    <div class="admin-login-form">
        <Heading1 Class="admin-heading" Size="TextSize.L">@I18N.T("Admin")</Heading1>
        <div class="form-inputs">
            <InputField ViewModel="VMEmail" Name="email" Label="@I18N.T("Email address verification")" Size="FormSize.M" Align="FormAlign.CENTER" Autocomplete />
        </div>
        <Button Size="ButtonSize.M" OnClick="Send">
            <Text>@I18N.T("Send")</Text>
        </Button>
        <WebLink
            Size="TextSize.S"
            NoWrap
            OnClick="() => VM.Show(LoginFormType.USER)"
            OnEnter="() => VM.Show(LoginFormType.USER)"
        >
            <AntIcon Type="login" Theme="outline" Class="icon-login" />&nbsp;@I18N.T("Login")&nbsp;
        </WebLink>
    </div>
} else {
    <Box>
        <div class="admin-login-form">
            <div class="verification-box">
                <AntIcon Type="mail" Theme="outline" Class="verification-icon" />
                <Paragraph Class="verification-text">@I18N.T("Check your email address.")</Paragraph>
                <Paragraph Class="verification-text-info" Size="TextSize.S">(@I18N.T("You can now close this page."))</Paragraph>
            </div>
        </div>
    </Box>
}
