@namespace Jumpeno.Client.Pages

@inherits Page

<div class="error-401-page">
    <DocTitle Value="@I18N.T("401 Not authenticated")" />
    <Heading1 Size="TextSize.XL" Class="error-page-heading">@I18N.T("Error 401")</Heading1>
    <Paragraph Size="TextSize.L" Class="error-page-paragraph" Spacing>@I18N.T("We apologize, you are not logged in.")</Paragraph>
    <Button
        Variant="ButtonVariant.Primary" Size="ButtonSize.M"
        Params="@(new ButtonLinkParams(
            href: I18N.Link<LoginPage>()
        ))"
    >
        <Icon><AntIcon Type="login" Theme="outline" /></Icon>
        <Text>@I18N.T("Log in")</Text>
    </Button>
</div>
