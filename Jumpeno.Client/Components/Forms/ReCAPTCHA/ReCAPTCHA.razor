@namespace Jumpeno.Client.Components

@inherits BasicComponent

<div
    class="@ComputeClass()"
    style="@Style"
    data-sitekey="@AppSettings.ReCAPTCHA.SiteKey"
    id="@captchaId"
    hidden="@(!Showing)"
/>
