@namespace Jumpeno.Client.Components

@typeparam T
@inherits RadioOption<T>

@code {
    protected override FormErrorType? ErrorType => FormErrorType.Connected;
    protected override RenderFragment RenderField() => @<Fragment>
        <button
            id="@ViewModel.FormID"
            class="@ClassElement"
            type="@ButtonType.Button.String()"
            role="radio"
            aria-label="@HTML.Label.Aria(Label, IsSelected)"
            aria-checked="@HTML.Label.AriaActive(IsSelected)"
            disabled="@Disabled"
            @onclick="Set"
        >
            <div class="@ClassContent">@ChildContent</div>
        </button>
    </Fragment>;
}

@Render()
