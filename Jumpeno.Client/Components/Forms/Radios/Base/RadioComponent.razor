@namespace Jumpeno.Client.Components

@typeparam T
@inherits FormField<RadioViewModel<T>>

@code {
    protected override FormErrorType? ErrorType => FormErrorType.Passive;
    protected override RenderFragment RenderField() => @<Fragment>
        <CascadingValue Name="@CascadeRef" TValue="RadioComponent<T>" Value="this" >
            <div class="@ClassGroup" role="radiogroup">
                @ChildContent
            </div>
        </CascadingValue>
    </Fragment>;
}

@Render()
