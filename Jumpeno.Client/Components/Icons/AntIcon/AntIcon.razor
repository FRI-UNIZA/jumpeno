@namespace Jumpeno.Client.Components

@inherits BasicComponent

<AntDesign.Icon
    Type="@Type"
    Theme="@MapTheme"
    Role="@ComputeRole()"
    AriaLabel="@ComputeLabel()"
    Class="@ComputeClass()"
    Style="@Style"
    @onclick="@OnClick"
    StopPropagation="@StopPropagation"
/>
