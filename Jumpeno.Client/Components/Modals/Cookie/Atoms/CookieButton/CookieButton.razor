@namespace Jumpeno.Client.Components

@inherits BasicComponent

<button
    type="@ButtonType.Button.String()"
    id="@ID"
    class="@ComputeClass()"
    style="@Style"
    @onclick="@OpenModal"
>
    <span>@I18N.T("Cookies")</span>
</button>
