@namespace Jumpeno.Client.Components

@inherits BasicComponent

<button
    id="@ComputeID()"
    type="@BUTTON_TYPE.BUTTON.String()"
    class="@ComputeClass()"
    style="@Style"
    @onclick="@Modal.Close"
    aria-label="@I18N.T("Close")"
    inert="@ComputeInert()"
>
    <AntIcon Type="close" Theme="outline" Class="@CLASS_ICON" />
</button>
