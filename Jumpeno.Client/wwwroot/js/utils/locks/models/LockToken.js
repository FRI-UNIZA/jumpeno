class LockToken {
    // Attributes -------------------------------------------------------------------------------------------------------------------------
    #Locked = true; Locked = () => this.#Locked
    #Unlock = () => {}

    // Lifecycle --------------------------------------------------------------------------------------------------------------------------
    constructor(unlock) {
        this.#Unlock = unlock
    }

    // Actions ----------------------------------------------------------------------------------------------------------------------------
    Unlock() {
        if (this.#Locked) this.#Unlock()
        this.#Locked = false
    }
}
